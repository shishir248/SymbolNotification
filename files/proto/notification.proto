syntax = "proto3";

package notifications;
option go_package = "./";

service PushNotification {
    rpc GetAccess (Access) returns (Subscription) {}
    rpc SendNotification (Notification) returns (Response) {}
}

message Access {
    bool access = 1;
}

message Subscription {
    bool access = 2;
}

message Notification {
    string title = 1;
    string message = 2;
}

message Response {
    string message = 1;
}
